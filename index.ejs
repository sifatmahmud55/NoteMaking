<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900 p-5">
        <div class="form p-10 text-white">
            <form action="/create" method="POST">
                <input class="block w-full rounded-lg px-3 py-2 bg-zinc-800 mt-2" type="text" placeholder="title goes here" name="title">
                <textarea class="block w-full rounded-lg px-3 py-2 bg-zinc-800 resize-none mt-2"
                    placeholder="write your details here" name="details"></textarea>
                <input class="mt-2 bg-blue-500 block rounded-lg px-3 py-2" type="submit">
            </form>

            <div class="tasks p-10 flex gap-5 flex-wrap">
                <% if (files.length > 0) { %>
                    <% files.forEach(function(file) { %>
                        <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-lg">
                            <h1 class="text-white text-3xl tracking-tight truncate w-full">
                                <%= file %>
                            </h1>
                            <div class="flex w-full justify-between"><a href="/file/<%= file %>" class="text-blue-500 inline-block mt-3" >read more</a>
                            <a href="/edit/<%= file %>" class="text-grey-500 inline-block mt-3">edit</a>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <h3 class="text-white">no tasks found</h3>
                <% } %>
            </div>
        </div>
    </div>
</body>

</html>
